import React from 'react';
import { Link } from "react-router-dom";
import { Button } from "@radix-ui/themes";

const Home = () => {
  return (
    <div className="starting screen">
      {/* Empty starting screen to maintain layout */}
    </div>
  );
};

export default Home;